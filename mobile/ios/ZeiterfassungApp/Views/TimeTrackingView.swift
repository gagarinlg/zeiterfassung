import SwiftUI

struct TimeTrackingView: View {
    var body: some View {
        NavigationView {
            Text("time_tracking_title".localized)
                .navigationTitle("nav_time_tracking".localized)
        }
    }
}
